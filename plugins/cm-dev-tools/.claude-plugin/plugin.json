{
  "name": "cm-dev-tools",
  "version": "1.1.2",
  "description": "Config Manager development toolkit: 7 workflow skills (scaffold, fleet review, release, PR lifecycle, parity check, PR comments, docs sync) + bash helper scripts + MCP server (8 tools with structured JSON I/O) + 2 custom agents. Uses built-in CLI agent capabilities (Copilot CLI / Claude Code), standard local tooling, and shell commands.",
  "skills": "./skills/",
  "agents": "./agents/"
}
